{
  "name": "khat-inventory-backend",
  "version": "1.0.0",
  "description": "Khat Inventory & Branch Management System API",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "npx prisma migrate deploy && npx prisma generate && node src/server.js",
    "dev": "nodemon src/server.js",
    "build": "prisma generate",
    "migrate": "prisma migrate deploy",
    "seed": "node prisma/seed.js",
    "migrate:dev": "prisma migrate dev",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^6.10.0",
    "helmet": "^7.0.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "prisma": "^5.7.0",
    "socket.io": "^4.6.1",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}